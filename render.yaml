services:
  - type: web
    name: Multi-Agent Research Copilot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn crew:app --host 0.0.0.0 --port $PORT
    plan: free
